# Default (plain HTTP) server. Used unless the deployment opts into TLS with
# docker-compose.tls.yml, which mounts infra/nginx-tls.conf over this file.
server {
    listen 80;
    server_name _;

    include /etc/nginx/vyzus-common.conf;
}
